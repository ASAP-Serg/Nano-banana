apiVersion: apps/v1
kind: Deployment
metadata:
  name: nano-banana-worker
  namespace: nano-banana
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nano-banana-worker
  template:
    metadata:
      labels:
        app: nano-banana-worker
    spec:
      containers:
        - name: worker
          image: cr.yandex/<registry>/nano-banana-backend:latest
          args: ["python", "-m", "nano_banana_worker"]
          envFrom:
            - configMapRef:
                name: nano-banana-config
            - secretRef:
                name: nano-banana-secrets
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 10001
          livenessProbe:
            exec:
              command:
                - python
                - -c
                - import redis; from nano_banana.config import settings; redis.from_url(settings.redis_url).ping()
            initialDelaySeconds: 15
            periodSeconds: 30
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
