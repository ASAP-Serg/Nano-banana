[project]
name = "nano-banana"
version = "2.0.0"
description = "Nano Banana image generation: API, worker, web"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["packages/core", "apps/api", "apps/worker"]
